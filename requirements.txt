# Compatibility install entry point.
#
# requirements.in is reviewed input. requirements.lock is the generated,
# hash-locked CPython 3.13.9 runtime resolution used by Docker and installers.
-r requirements.lock
